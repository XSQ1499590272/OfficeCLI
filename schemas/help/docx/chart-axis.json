{
  "$schema": "../_schema.json",
  "format": "docx",
  "element": "chart-axis",
  "parent": "chart",
  "operations": {
    "add": false,
    "set": true,
    "get": true,
    "remove": false
  },
  "note": "这是 pptx/chart-axis 的镜像。axis 会随 chartType 变化隐式创建或销毁，不提供直接 Add/Remove。创建 chart 时通过 chart 的 axis* props（axismin/axismax/axistitle/axisfont/…）配置；chart-axis 仅覆盖创建后的操作。已知限制：labelFont 写入 title run，而不是 tick labels；lineWidth/lineDash 应用到所有 plot-area series；要设置单个 series 的线型，请使用 chart-series。",
  "addressing": {
    "key": "role",
    "pathForm": "/chart[N]/axis[@role=ROLE]",
    "keyValues": [
      "category",
      "value",
      "value2",
      "series"
    ]
  },
  "extends": "_shared/chart-axis"
}
