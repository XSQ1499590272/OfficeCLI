{
  "$schema": "../_schema.json",
  "element": "table-row",
  "shared_base": true,
  "properties": {
    "cols": {
      "type": "int",
      "description": "断言新行的单元格数与表格网格一致。该行在结构上受现有 <a:tblGrid> 约束；如需加宽，请先添加列（或在单元格 1 上设置 gridSpan 以合并为更宽的跨度）。省略时默认采用现有网格列数。",
      "add": true,
      "set": false,
      "get": false,
      "examples": [
        "--prop cols=2"
      ],
      "readback": "n/a (structural — cell count surfaces via DocumentNode.Children, not Format)",
      "enforcement": "strict"
    },
    "height": {
      "type": "length",
      "description": "行高，接受可解析为 EMU 的长度。默认使用首行高度，或约为 1cm。",
      "add": true,
      "set": true,
      "get": true,
      "examples": [
        "--prop height=1cm",
        "--prop height=500"
      ],
      "readback": "length in cm (e.g. \"2cm\")",
      "enforcement": "report"
    }
  }
}
